@typeparam T
@implements IGridCol<T>
@inherits ColBase
@if (Register == null)
{
    <div @attributes="FinalAttributes">
        @if (ChildContent == null)
        {
            @GetAutoValue()
        }
        else
        {
            @ChildContent
        }
    </div>
}