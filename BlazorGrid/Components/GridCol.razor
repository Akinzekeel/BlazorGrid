@typeparam T
@implements IGridCol<T>
@if (Register == null)
{
    <div @attributes="FinalAttributes">
        @if (ChildContent == null)
        {
            @GetAutoValue()
        }
        else
        {
            @ChildContent
        }
    </div>
}